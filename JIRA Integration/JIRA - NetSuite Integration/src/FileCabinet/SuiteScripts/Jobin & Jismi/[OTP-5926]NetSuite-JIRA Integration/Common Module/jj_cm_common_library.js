/**
 * @NApiVersion 2.1
 */
define(['N/http', 'N/record', 'N/search'],
    /**
 * @param{http} http
 * @param{record} record
 * @param{search} search
 */
    (http, record, search) => {

        const foo = () => {

        }

        const bar = () => {

        }

        return {foo, bar}

    });
